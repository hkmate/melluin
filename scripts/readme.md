Most of the scripts in this directory are used only in deployment
process for example from dockerfile.

An exception is [docker-compose.yml](./docker-compose.yml) what is for create a dev environment.
About that, you can take a look on [README.md](../README.md#install-the-project) in project root.